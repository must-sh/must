
extern fn print(x: int) -> int;
extern fn read() -> int;

fn square(x: int) -> int {
    x * x
}

fn is_even(x: int) -> bool {
    (x / 2) * 2 == x
}

fn main() -> int {
    let mut i = 10 in
    while 0 < i {
        let _ = print(i) in
        i = i - 1
    }
}
