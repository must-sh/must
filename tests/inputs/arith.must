
extern fn must_print(x: int);
extern fn must_read() -> int;

fn square(x: int) -> int {
    x * x
}

fn is_even(x: int) -> bool {
    (x / 2) * 2 == x
}

fn main() {
    let mut i = must_read() in
    while 0 < i {
        must_print(i);
        i = i - 1
    }
}
