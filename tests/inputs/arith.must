
extern fn print(x: int);
extern fn read() -> int;

fn square(x: int) -> int {
    x * x
}

fn is_even(x: int) -> bool {
    (x / 2) * 2 == x
}

fn main() {
    let mut i = 10 in
    while 0 < i {
        print(i);
        i = i - 1
    }
}
