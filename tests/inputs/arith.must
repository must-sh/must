
extern fn print(x: int) -> int;
extern fn read() -> int;

fn square(x: int) -> int {
    x * x
}

fn is_even(x: int) -> bool {
    (x / 2) * 2 == x
}

fn main() -> int {
    let _ = while is_even(read()) {
        print(40)
    }
    in
    9
}
