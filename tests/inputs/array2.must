
extern fn must_print(x: int);

fn make_arr() -> [2]int {
    [1, 2]
}

fn modify(arr: *mut [2]int) {
    (*arr)[1] = 42
}

fn arr_sum(arr: [2]int) -> int {
    arr[0] + arr[1]
}

fn main() {
    let mut arr = make_arr() in
    arr[0] = 13;
    modify(&arr);
    must_print(arr_sum(arr))
}
