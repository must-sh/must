
fn swap(x: *mut int, y: *mut int) -> int {
    let tmp = *x in
    let _ = *x = *y in
    let _ = *y = tmp in
    0
}

fn main() -> int {
    let mut x = 42 in
    let mut y = 13 in
    let _ = swap(&x, &y) in
    x
}
